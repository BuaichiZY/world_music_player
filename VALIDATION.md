# 验证记录

验证日期：2026-09-13。工程版本：1.0.1。

## 环境

- Windows 11 amd64。
- 本机全局 Gradle 9.6.1。
- Eclipse Adoptium Java 25，编译目标 25（class major version 69）。
- Minecraft 26.1.2，NeoForge 26.1.2.94，ModDevGradle 2.0.144。

## 已通过的检查

1. `gradle build`：编译主代码、处理资源、生成安装 JAR 与源码 JAR，并执行音乐逻辑验证。
2. `verifyMusicLogic`：四个时段的开始和结束边界、24000 刻跨天、负时间取模、长时间存档；空歌单、单曲、重复 ID；10000 次随机选择不连续重复且覆盖所有 10 个槽位；第 11 个槽位不会播放。同一时段不会获得第二次播放机会；新时段、次日相同时段及新连接会正确重置播放机会。
3. `runClientSmoke`：实际启动 26.1.2 / 26.1.2.94 客户端；模组与配置正常加载；12 个默认单曲事件都能解析到音频；空歌单、10 首歌单有效；配置 UI 的列表上限为 10；非法声音 ID 被配置验证拒绝。
4. `runClientGameplay`：自动建立独立单人世界，在游戏时间 0、6000、12000、18000、24000 刻验证对应音乐事件及其实际音频通道处于活动状态，检查上一时段曲目停止。手动结束音频后不会再次播放。在 30000 刻从两个短音频候选中播放一首，等待其自然结束，确认没有第二次音乐播放事件（包括原版音乐）；修改为空歌单、再改回非空歌单仍保持安静；到 36000 刻的新时段恢复播放。
5. 音乐打包工具的 1.0.0 基线检查：使用本机游戏缓存中的短 Ogg 音效作为四组临时输入，验证中文和空格文件名、ZIP 根目录、84.0 资源包格式、流式声音条目、四个音频文件及对应 TOML 歌单；空输入和 11 首输入均在生成资源包前拒绝。1.0.1 仅移除了生成配置中的 `gapSeconds`。
6. 发布包检查：JAR 元数据和版本约束正确；包含主代码、声音映射及中英翻译；不包含开发测试模组、测试世界或音频文件。

开发测试输出分别在 `run/smoke/smoke-result.txt`、`run/gameplay/gameplay-result.txt`，运行日志位于相应目录的 `logs`。`src/smoke` 中的测试代码仅供开发环境，不进入发布 JAR。

## 验证范围

音频通道状态由真实客户端音频引擎检查，未进行人工听感评价。未逐个测试后续 NeoForge 构建、多模组整合包、每一种外部音频编码、多人服务器及所有自定义维度。第三方模组若同时接管背景音乐，仍需在目标整合包验证。

该版本不含 MP3/WAV/Opus 解码器；自定义文件使用 Ogg Vorbis。默认音频仅引用游戏资源，发布 JAR 不复制或附送游戏音乐。
